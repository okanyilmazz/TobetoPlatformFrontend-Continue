import { Identifier } from "typescript";

export default interface UpdatedAccountFavoriteEducationProgramResponse {
    id: Identifier;
    accountId: Identifier;
    educationProgramId: Identifier;
}